import { createAction } from 'redux-actions'

import * as types from './types'

export const setSalesChannels = createAction(types.SET_SALES_CHANNELS)
export const setChannels = createAction(types.SET_CHANNELS)
